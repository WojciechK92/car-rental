server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:testDb;DB_CLOSE_DELAY=-1"
    username: "sa"
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop